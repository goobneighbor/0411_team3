<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.t09ethr.dao.OnlineListDAO">
	<!-- 
	<select id="productDetailSelect" resultType="com.t09ether.home.dto.OnlineDTO">
		select d.on_no, p.pro_code, d.on_count, d.find_addr, d.hit, d.writedate ,p.pro_name, p.pro_price, p.pro_stock, p.image, p.detailed from product_detail d join product p on d.pro_code=p.pro_code and d.pro_code=${param1} 
	</select>
	 -->
	 
</mapper>